# Список завдань (Tasks)

## Виконані завдання
- [x] Реалізувати прямий обхід усіх елементів `showcase_data` при `--reset` без залежності від ключів `target_groups`.
- [x] Додати покроковий вивід у консоль для видалення (`🗑 Видалено`) та публікації (`📤 [N/30] Опубліковано`).
- [x] Прогнати всі 28 тестів паралельно та випустити реліз `v0.7.36`.
