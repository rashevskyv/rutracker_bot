# Завдання (Tasks): Виправлення eShop Live Showcase

## Виконані завдання
- [x] **Виправлення синхронізації Gist (`sync_gist_state.py`)**:
  - [x] Абсолютні шляхи для `DATA_DIR`.
  - [x] Логіка об'єднання (merge) для `eshop_active_showcase.json` (локальний стан із новішими timestamp/message_id перемагає).
  - [x] Логіка об'єднання для `eshop_posted_deals.json` та `last_eshop_deals_run.json`.
- [x] **Відновлення expiration-based ротації вітрини (`send_eshop_deals.py`)**:
  - [x] Абсолютні шляхи для файлів стану.
  - [x] Діагностичний вивід параметрів файлу вітрини на старті (`log_showcase_diagnostics`).
  - [x] Перевірка чинності знижки для кожної збереженої гри через `get_game_by_fs_id` / пошук.
  - [x] Видалення карток для знижок, що закінчилися / значно змінилася ціна.
  - [x] Добір тільки необхідної кількості нових ігор (`max_active_showcase - len(surviving)`).
  - [x] Миттєве збереження стану після кожної успішно опублікованої картки.
  - [x] 0 публікацій, якщо вітрина заповнена.
  - [x] Додавання CLI прапорця `--delete-messages` для видалення діапазонів або списків ID орфанних повідомлень.
  - [x] Безпека UTF-8 виводу в Windows-консолі (cp1251).
- [x] **Тести (`test_eshop_module.py`, `test_gist_config.py`)**:
  - [x] Тести expiration логіки, збереження, нульової публікації при повній вітрині, persist message_ids.
  - [x] Тести для правил злиття Gist.
  - [x] 35/35 тестів пройшли успішно у паралельному режимі (`pytest -n auto`).
- [x] **Документація**:
  - [x] Оновлення `README.md`, `CHANGELOG.md` (v0.7.44), `GEMINI.md`, `Walkthrough.md`, `plan.md`.
- [x] **Фінальний звіт**:
  - [x] Підготовка підсумкового звіту для автора інструкцій.
