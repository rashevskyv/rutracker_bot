# Завдання: Стиснення та санітизація ченджлогів і описів у Homebrew Digest

## Виконані завдання
- [x] **Санітизація та стиснення у форматтері (`digest/homebrew.py`)**:
  - [x] Реалізовано функцію `sanitize_digest_description(description: str) -> str` для видалення зайвої розмітки, сирого markdown (`**bold**`, `* bullet`, `### heading`, `[text](url)`), об'єднання переносів рядків та скорочення основного опису та ченджлогу до 1-2 речень.
  - [x] Застосовано санітайзер у `format_digest_message` до кожного запису перед додаванням у рядок дайджесту.
- [x] **Виправлення вибору опису та валідації перекладу (`collect_homebrew_updates.py`)**:
  - [x] Встановлено пріоритет короткого опису (`description`) над довгим (`long_description`/`details`) для UDB, ForTheUsers, VitaDB.
  - [x] Додано валідацію результату `translate_short_description` у `_get_description_cached`: заборона кешування сирого англійського тексту, перевірка наявності кирилиці, повернення чистого компактного фолбека `Додаток {fallback_name}.`.
  - [x] Додано очищення вхідних/вихідних даних у `summarize_and_translate_notes` (видалення HTML, markdown-розмітки, обмеження до 1-2 речень).
- [x] **Виправлення сервісу перекладу (`services/translation.py`)**:
  - [x] У `translate_short_description` повертається порожній рядок у разі збою LLM (замість повернення 5000 символів сирого вхідного markdown).
  - [x] Додано очищення від залишків Markdown та валідацію кешу.
- [x] **Очищення наявних даних (`data/homebrew_digest_data.json`, `data/hb_descriptions.json`)**:
  - [x] Очищено запис SysMon (індекс 511) від 15-рядкового сирого markdown-мануалу, встановлено коректний 1-рядковий опис та ченджлог в 1 речення.
  - [x] Очищено невалідні англійські/багаторядкові записи в `data/hb_descriptions.json`.
- [x] **Тестування (`test_homebrew_digest_formatting.py`)**:
  - [x] Написано юніт-тести для санітизації, скорочення до 1-2 речень, валідації фолбеків та форматування повідомлення дайджесту.
  - [x] 47/47 тестів пройшли успішно у паралельному режимі (`pytest -n auto`).
- [x] **Документація та реліз**:
  - [x] Оновлено `README.md`, `CHANGELOG.md` (v0.7.46), `GEMINI.md`, `walkthrough.md`, `plan.md`.
