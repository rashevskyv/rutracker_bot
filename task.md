# Список завдань (Tasks)

## Виконані завдання
- [x] Виправити резолвінг `target_topic` у `send_eshop_deals.py` (гарантовано `561344` для чату `-1001790782971`).
- [x] Реалізувати фізичне зондування та видалення всіх наявних повідомлень у топіку `561344` (від `top_id` до `561345`).
- [x] Прогнати всі 18 тестів паралельно та випустити реліз `v0.7.10`.
