# Список завдань (Tasks)

## Виконані завдання
- [x] Додати `reply_to_message_id` та `message_thread_id` до всіх викликів `cmd_deals` та `cmd_search`.
- [x] Додати підтримку топіків/гілок у `add_subscription` та `cmd_subscribe`.
- [x] Прогнати тести та випустити реліз `v0.6.88`.
