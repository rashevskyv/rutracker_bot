# Список завдань (Tasks)

## Виконані завдання
- [x] Реалізувати глибоке зондування та рекурсивне сканування теми при `remove all` / `remove 20` для видалення всіх невідстежених повідомлень.
- [x] Оновити `send_eshop_deals.py` та `bot_commands.py`.
- [x] Прогнати всі 17 тестів паралельно та випустити реліз `v0.7.05`.
