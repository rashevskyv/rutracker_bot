# Список завдань (Tasks)

## Виконані завдання
- [x] Виправити роботу фільтрації цін у гривнях та рангу популярності в `get_flexible_deals` для `/deals` та `/random`.
- [x] Додати метод `convert` у `CurrencyService` та підтримку `min_price_eur`/`max_price_eur` у Solr API.
- [x] Прогнати всі 20 тестів паралельно та випустити реліз `v0.7.20`.
