# Список завдань (Tasks)

## Виконані завдання
- [x] Додати виклик `await close_clients()` у `finally:` блоці `send_eshop_deals.py` для очищення неперерваних сесій aiohttp.
- [x] Прогнати всі 15 тестів паралельно та випустити реліз `v0.6.96`.
