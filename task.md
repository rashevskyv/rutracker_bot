# Список завдань (Tasks)

## Виконані завдання
- [x] Реалізувати мульти-ключову розумну дедуплікацію для cron розсилки знижок (`fs_id`, `nsuid`, `normalized_title`) з кулдауном 14 днів.
- [x] Додати тест `test_cron_deals_deduplication` у `test_eshop_module.py`.
- [x] Прогнати всі 15 тестів паралельно та випустити реліз `v0.6.94`.
