# Список завдань (Tasks)

## Виконані завдання
- [x] Виправити порядок імпорту `IS_TEST_MODE` у `cmd_remove_deals` в `services/eshop/bot_commands.py`.
- [x] Додати тест `test_cmd_remove_deals`, прогнати всі 22 тести паралельно та випустити реліз `v0.7.25`.
