# Список завдань (Tasks)

## Виконані завдання
- [x] Реалізувати детекцію жанру Homebrew (`is_homebrew_genre`) для роздач RuTracker (підтримка `Homebrew`, `#Homebrew`, `Хоумбрю`, `Хомбрю`, тегів у назві та хештегів).
- [x] Вимкнути пошук та завантаження скріншотів у `main.py` для роздач із жанром Homebrew.
- [x] Написати юніт-тести для `is_homebrew_genre` (`test_tracker_homebrew.py`).
- [x] Прогнати всі 28 тестів паралельно (`pytest -v -n auto`).
- [x] Оновити документацію (`README.md`, `CHANGELOG.md`, `plan.md`, `walkthrough.md`) та ітерувати версію до `v0.7.33`.
