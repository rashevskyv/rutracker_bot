# Список завдань (Tasks)

## Виконані завдання
- [x] Реалізувати миттєве атомарне збереження `eshop_active_showcase.json` та `eshop_posted_deals.json` після кожного відправленого повідомлення.
- [x] Запобігти втраті `message_id` при перериваннях і збоях, усунувши повторні дублювання.
- [x] Прогнати всі 28 тестів паралельно та випустити реліз `v0.7.38`.
