# План інтеграції перевірки оновлень оброблених ручних релізів у колекторі (v0.6.47)

Цей план описує процес впровадження перевірки подальших оновлень на GitHub/GitLab для оброблених ручних релізів (`processed: true` з `manual_releases.json`).

## Пропоновані зміни

### 1. Допрацювання колектора homebrew оновлень
- [x] Модифікувати `load_homebrew_list` у `collect_homebrew_updates.py` для автоматичного завантаження та об'єднання оброблених ручних релізів (`processed: true`) із файлу `data/manual_releases.json`.
- [x] Налаштувати відображення оновлень: для існуючих/оброблених релізів встановлювати `is_new: false` (з маркером звичайного оновлення `•`).
- [x] Додати автоматичне оновлення версії, посилання та дати в `manual_releases.json` та `hb_state.json` при знаходженні нового тегу на GitHub.
- [x] Протестувати роботу колектора.

### 2. Версіонування та документація
- [x] Оновити `CHANGELOG.md` (англійською мовою), зафіксувавши версію `v0.6.47`.
- [x] Оновити `task.md`, `walkthrough.md` та `plan.md` (українською мовою).
- [x] Виконати git commit та створити новий тег `v0.6.47`.
