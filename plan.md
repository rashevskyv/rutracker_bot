# План виправлення полів processed та автоматичного збору релізів NaGaa95 (v0.6.43)

Цей план описує виправлення структури записів у файлі ручних релізів `manual_releases.json` та збір нових портів.

## Пропоновані зміни

### 1. Виправлення скрипта збору `collect_nagaa_releases.py`
- [x] Додати обов'язкове поле `"processed": false` при формуванні нових записів.
- [x] Переконатись, що під час генерації нових релізів структура даних є стабільною і відповідає загальній схемі.

### 2. Патч бази даних ручних релізів у Gist
- [x] Виправити існуючі записи в `data/manual_releases.json`, які не містили ключа `"processed"`, встановивши їм значення `false`.
- [x] Виконати синхронізацію та завантажити виправлену версію бази даних у хмарний Gist за допомогою `python sync_gist_state.py upload`.

### 3. Автоматичний збір нових релізів
- [x] Запустити скрипт `collect_nagaa_releases.py` для автоматичного збору нових репозиторіїв та релізів користувача `NaGaa95`.
- [x] Переконатися в успішному імпорті та автоматичному вивантаженні результатів у Gist.

### 4. Версіонування та документація
- [x] Оновити `CHANGELOG.md` (англійською мовою), зафіксувавши версію `v0.6.43`.
- [x] Оновити `task.md`, `walkthrough.md` та `plan.md` (українською мовою).
- [x] Виконати git commit та створити тег `v0.6.43`.
