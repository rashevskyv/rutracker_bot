# План розвитку: RuTracker Bot

## Загальний стан проєкту
RuTracker Bot забезпечує моніторинг релізів трекера, збір оновлень хоумбрю, перекладів українською (SWUK), ручних релізів та знижок Nintendo eShop з мультирегіональним порівнянням цін (топ-3 + гарантовані **ПАР 🇿🇦, Тайланд 🇹🇭, Польща 🇵🇱, Норвегія 🇳🇴, США 🇺🇸**), клікабельними прямими посиланнями на регіональні eShop, стійким 12-годинним кешуванням та захистом від рейт-лімітів, детекцією платформи Switch 1/2, графічними HD-бейджами платформ на обкладинках (без артефактів стиснення), фокусом на 100% популярних хітах (без сміття), інтерактивним списком бажань (Wishlist) з автосповіщеннями, гранулярними користувацькими підписками у приватних повідомленнях (`/subscriptions`), примусовим скиданням та перепублікацією вітрини (`--reset`, `--refresh`, `reset`), підтримкою кириличних аліасів команд (`/допомога`, `/знижки`, `/пошук`, `/вішліст`, `/підписки`), автоматичним зняттям кулдауну при видаленні ігор, ізольованим очищенням вітрини через CLI (`--remove all`, `--remove 30`) та Telegram-команду (`/remove`) за точними збереженими ID повідомлень, централізованим рівнем безпеки `safe_delete_showcase_message` із суворим обмеженням видалення виключно топіком `561344` (`https://t.me/kefir_ukr/561344`), надійною відправкою (`allow_sending_without_reply`), живою вітриною до 30 ігор з авто-перевіркою та авто-видаленням протермінованих знижок, посиланнями на eShop-Prices.com, потоковою відправкою у відповідь та в конкретні топіки/гілки, авторозсилкою в призначений топік `561344`, чітким маркуванням регіонів, хештегами жанрів, глобальним мульти-ключовим кешуванням перекладів та ШІ-обробкою через OpenRouter.

---

## Етапи та модулі

### Модуль 1: RuTracker Feed & Updates (OpenRouter / Luna / DeepSeek / Caching) [Виконано]
- Моніторинг Atom-стрічки, парсинг описів, переклад RU->UA через `openai/gpt-5.6-luna` з fallback на `deepseek/deepseek-v4-flash-0731`, SHA-256 кешування перекладів, валідація трейлерів YouTube. Підтримка доставки репостів роздач підписаним користувачам у приватні повідомлення.

### Модуль 2: Збір хоумбрю та локалізацій [Виконано]
- Підтримка Universal-DB, ForTheUsers, VitaForge, SwitchPorts, SWUK та користувацьких репозиторіїв із генерацією підсумків оновлень через OpenRouter та кешуванням у `hb_descriptions.json`.

### Модуль 3: Щоденні дайджести (Daily, Homebrew, Swuk) [Виконано]
- Агрегація та щоденна розсилка дайджестів у налаштовані канали/групи та особисто підписникам категорії `digests`.

### Модуль 4: Nintendo eShop Deals & Wishlist Module (v0.7.16) [Виконано]
- [x] Автоматичний моніторинг офіційних знижок Nintendo Switch без блокувань Cloudflare.
- [x] Алгоритм відбору знижок за каталогом 200+ найпопулярніших хітів та провідних видавців (100% якісні ігри, без shovelware).
- [x] **Підтримка кириличних команд**: `/допомога`, `/старт`, `/хелп`, `/знижки`, `/пошук`, `/вішліст`, `/підписки`, `/підписатися`, `/відписатися`, `/видалити`.
- [x] **Жива вітрина до 30 ігор (Live Showcase Rotation) з авто-видаленням протермінованих знижок**:
  - Розмір активної вітрини збільшено до 30 ігор.
  - Бот перевіряє статус кожної розміщеної гри за її унікальним `fs_id`.
  - Якщо знижка закінчилася, бот викликає `safe_delete_showcase_message()` і видаляє старий пост з Telegram-теми.
  - Звільнені місця дозаповнюються свіжими знижками до ліміту 30.
- [x] **Автоматичне зняття кулдауну при видаленні ігор з вітрини**:
  - При `remove all` чи видаленні окремої гри бот знімає кулдаун у `data/eshop_posted_deals.json`, щоб наступний запуск міг знову обрати топові позиції.
- [x] **Примусове скидання та публікація 30 свіжих повідомлень (`--reset`, `reset`, `--refresh`)**:
  - Можливість примусово очистити локальну базу вітрини та кулдаун і опублікувати 30 нових ігор з нуля однією командою.
- [x] **Гарантовані регіони (ПАР, Тайланд, Польща, Норвегія)**:
  - У кожному повідомленні зі знижками під топ-3 найдешевшими регіонами завжди відображаються: **🇿🇦 ПАР, 🇹🇭 Тайланд, 🇵🇱 Польща, 🇳🇴 Норвегія** (та 🇺🇸 США) з прямими гіперпосиланнями на регіональні магазини eShop.
- [x] **Видалення виключно за точними зафіксованими `message_id`**:
  - Повністю ліквідовано сканування числових діапазонів `range()`.
  - Видаляються виключно ті ID повідомлень, які бот зберіг при публікації в топік `561344`.
- [x] **Централізований безпечний шлюз (`safe_delete_showcase_message`)**:
  - Усі операції видалення заблоковані для будь-яких інших чатів та тем, і діють **виключно у гілці `https://t.me/kefir_ukr/561344`**.
  - Покриття автоматичним юніт-тестом `test_strict_deletion_guardrail`.
- [x] **Клікабельні регіональні ціни**: кожна ціна в картці є прямим гіперпосиланням на магазин eShop відповідної країни (🇺🇸 США, 🇵🇱 Польща, 🇦🇷 Аргентина, 🇨🇱 Чилі тощо).
- [x] **Прицільний роутинг та нульова надмірність (Showcase-First & Lazy Enrichment)**:
  - Авторозсилка вітрини знижок спрямовується **виключно в призначений топік `561344`**.
  - Якщо у вітрині вже є 30 активних ігор, крон миттєво завершує роботу за 0.5с без жодних звернень до AI чи зайвих перекладів.
  - Збагачення (AI-переклад та ціни в регіонах) викликається лише для вільних слотів.
- [x] **Надійне збереження блоку регіональних цін (`🌍 Ціни в регіонах eShop:`)**:
  - Додано 12-годинний персистентний кеш `data/eshop_region_prices_cache.json` для виключення дублюючих запитів.
  - Введено контроль паралелізму `asyncio.Semaphore(4)` для запобігання тимчасовим 403-блокуванням від Akamai.
  - Додано Algolia US Price Fallback, завдяки чому американські ціни (USD) витягуються гарантовано за будь-яких умов.
- [x] **Кришталево чисті HD-обкладинки та векторний 2x supersampling редизайн бейджів платформ**:
  - Автоматичне завантаження `1600w` та `1200w` оригінальних HD-асетів з Nintendo CDN.
  - Редизайн у сучасний, компактний та елегантний стиль (заокруглений pill-шейп з м'якою тінню та акуратними Joy-Con іконками).
  - Збереження у максимальній якості `quality=96`, `subsampling=0` (4:4:4) без артефактів стиснення.
- [x] **Прямі посилання на `eShop-Prices.com`** у футері кожної картки (`🛒 Nintendo eShop | 🌐 eShop-Prices.com`).
- [x] **Персональний список бажань (Wishlist)**: команди `/wishlist`, `/wishlist add`, `/wishlist remove`, `/wishlist clear` з моніторингом цін у реальному часі.
- [x] **Гранулярні підписки в приватних повідомленнях (`/subscriptions`, `/sub`, `/unsub`)**:
  - За замовченням усі авто-пости у приватних **вимкнено** (бот відповідає лише на команди користувача).
  - Можливість увімкнути/вимкнути окремо: знижки (`deals`), репости з RuTracker (`rutracker`), дайджести (`digests`) або всі разом (`all`).
- [x] **Захист від помилок 400 при відповідях**: `allow_sending_without_reply=True` та автоматичний fallback у `safe_reply` і `safe_send_card`.
- [x] **Чисте форматування цін без знижок**: відображення чистої поточної ціни без закреслення та без `(-0%)`.
- [x] **Коректне закриття async клієнтів (`close_clients`)** при завершенні роботи крону.
- [x] **Автоматичні сповіщення про знижки на ігри з вішліста** в щогодинному/періодичному кроні.
- [x] Потокова послідовна відправка у `/deals` та `/search` у реальному часі безпосередньо **у відповідь на повідомлення користувача** та в **активний топік/гілку форуму**.
- [x] Автоматична розсилка по крону в призначений топік **`561344`**.
- [x] Чітке маркування базової ціни каталогу (`🇪🇺 Європа:`).
- [x] Глобальне мульти-ключове кешування описів ігор (SHA-256 + назва + fs_id) із логуванням `[CACHE HIT]` / `[CACHE MISS]`.
- [x] Дводіапазонний пошук цін: резолвінг як європейських, так і американських NSUID.
- [x] Мультирегіональне порівняння цін: топ-3 найдешевші регіони у світі (🥇, 🥈, 🥉) + гарантовані **Польща 🇵🇱 та США 🇺🇸**.
- [x] Автоматична конвертація в **гривні (грн / ₴)** для базової ціни та всіх регіональних цін.
- [x] Автоматичний переклад синопсисів/описів ігор на українську мову через OpenRouter з кешуванням у `translations_cache.json`.
- [x] Вивід жанрів оригінальними англійськими хештегами (`🏷 #Action #Adventure #RPG`).
- [x] Динамічні графічні бейджі платформ на обкладинках (Switch, Switch 2 Exclusive, Switch 1 & 2).
- [x] Інтервальний крон розсилки топ-знижок (`interval_hours`, `--force`).
- [x] Інтеграція оцінок Metacritic та RAWG для відсіювання сміття.
- [x] Інтерактивні команди в чатах через `bot_interactive.py`.
