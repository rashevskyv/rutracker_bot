# Звіт про виконану роботу: Збільшення місткості вітрини до 30 ігор (v0.7.15)

## Огляд задачі
Користувач попросив збільшити місткість живої вітрини та кількість ігор за замовчуванням при запуску (зокрема через `--force` чи крон) з 20 до 30 ігор.

---

## Виконані кроки

1. **Збільшення лімітів у конфігурації та коді**:
   - `config/settings.json`:
     - `"max_deals_per_run": 30`
     - `"max_active_showcase": 30`
   - `send_eshop_deals.py`:
     - Оновлено дефолтний розмір пулу до 30 ігор.
   - `README.md`:
     - Оновлено документацію та приклади команд для 30 ігор.

2. **Тестування**:
   - Усі 18 тестів пройшли успішно (`pytest -v -n auto`).
