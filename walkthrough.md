# Звіт про виконану роботу: Автоматична перевірка валідності та видалення неактивних знижок (v0.7.03)

## Огляд задачі
Роз'яснити та забезпечити надійну роботу автоматичної перевірки активних знижок у темі та видалення протермінованих пропозицій.

---

## Як влаштований життєвий цикл вітрини

1. **Фаза 1: Перевірка наявних 20 ігор (Видалення неактивних)**:
   - Крон бере кожну гру, яка зараз розміщена у вітрині (`data/eshop_active_showcase.json`), і запитує офіційний eShop за унікальним `fs_id` / назвою (`eshop_service.get_game_by_fs_id`).
   - Якщо акція на гру закінчилася (ціна повернулася до звичайної) ➡️ бот автоматично викликає `bot.delete_message(chat_id, message_id)` і **видаляє це повідомлення з Telegram-теми**.

2. **Фаза 2: Дозаповнення звільнених слотів**:
   - Якщо, наприклад, 3 знижки закінчилися і 3 повідомлення видалено, бот бачить: `available_slots = 3`.
   - Тільки в цей момент бот шукає 3 нові топ-хіти зі знижками, перекладає їхні описи та публікує нові повідомлення у тему, записуючи їхні `message_id` у файл вітрини.

3. **Якщо всі 20 знижок досі активні**:
   - Бот перевірив їх, побачив що всі 20 акцій ще діють ➡️ нічого не видаляє і не спамить новими іграми, завершуючи цикл без зайвих витрат AI-токенів.
