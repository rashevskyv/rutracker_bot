# Результати виконання — Автоматичний збір усіх нових релізів від вчорашнього дня (v0.6.51)

Спрощено логіку кастомного колектора `collect_custom_releases.py`: тепер автоматично збираються **усі нові релізи**, опубліковані авторами `NaGaa95` та `ChanseyIsTheBest`, починаючи від вчорашнього дня (за останні ~48 годин).

## Зміни, що були внесені (v0.6.51)

### 1. Збір усіх релізів від вчорашнього дня (`is_release_since_yesterday`)
- Вилучено будь-які складні фільтри категорій чи AI-класифікації ігор.
- Перевіряється тільки точна дата публікації тегу/релізу: якщо новий випуск опубліковано починаючи від вчорашнього дня (`date >= yesterday`), він автоматично потрапляє до дайджесту ручних релізів.
- Для нових релізів **Gemini 3.5 Flash thinking** форматує охайну назву застосунку та генерує стислий опис українською мовою.

### 2. Версіонування та документація
- Версію програми оновлено до `v0.6.51` у файлах `CHANGELOG.md`, `task.md` та `walkthrough.md`.
