# Звіт про виконану роботу: Гарантоване відображення регіонів ПАР, Тайланд, Польща, Норвегія (v0.7.12)

## Огляд задачі
Користувач встановив обов'язковий перелік регіонів, які завжди мають відображатися в кожному повідомленні зі знижками:
1. 🇿🇦 **ПАР (South Africa - ZA)**
2. 🇹🇭 **Тайланд (Thailand - TH)**
3. 🇵🇱 **Польща (Poland - PL)**
4. 🇳🇴 **Норвегія (Norway - NO)**

---

## Виконані кроки

1. **Оновлення списку закріплених регіонів (`pinned_targets`)**:
   - `services/eshop/formatters.py` та `eshop-prices/src/bot/formatters.py`:
     - Закріплено список регіонів: `ZA` (🇿🇦 ПАР), `TH` (🇹🇭 Тайланд), `PL` (🇵🇱 Польща), `NO` (🇳🇴 Норвегія), `US` (🇺🇸 США).
     - Якщо будь-який із цих регіонів не потрапив у топ-3 найдешевших у світі, він гарантовано додається окремим рядком із прямим клікабельним посиланням на локальний eShop та конвертацією в гривні.

2. **Підтримка азійських регіонів у сервісі цін**:
   - `services/eshop/region_price_service.py`:
     - Додано `ASIA_OTHER_REGIONS` (`TH`, `HK`, `JP`) до `TRACKED_REGIONS`.

3. **Тестування**:
   - Оновлено тести в `test_eshop_module.py` для верифікації наявності всіх 4 регіонів у згенерованому тексті картки.
   - Усі 18 тестів пройшли успішно (`pytest -v -n auto`).
