# Звіт про виконану роботу: Відображення еквівалента в гривнях для верхньої ціни (v0.6.74)

## Огляд задачі
Користувач звернув увагу на те, що в картці гри верхній базовий рядок ціни (наприклад `💰 6.99 EUR ➡️ 1.39 EUR (-80%)`) ще не містив підрахунку в гривнях, на відміну від нижнього регіонального блоку.

---

## Виконані кроки

1. **Оновлення форматера базової ціни (`formatters.py`)**:
   - Верхній рядок тепер містить повний підрахунок вартості в гривнях та доларах:
     `💰 <s>6.99 EUR</s> ➡️ <b>1.39 EUR</b> (<b>-80%</b>) (<i>~62 грн / $1.51</i>)`
   - Виправлено відображення внутрішнього службового плейсхолдера відсутності рейтингу Nintendo (`#999999`) — тепер показується коректний статус `Без оцінки`.

2. **Підключення до інтерактивних та фонових скриптів**:
   - Оновлено `bot_commands.py`, `bot_interactive.py` та `send_eshop_deals.py` для передачі актуального сервісу курсів `currency_service` у форматер.

3. **Тестування**:
   - Усі 10 паралельних тестів пройшли успішно (`pytest -v -n auto`).
