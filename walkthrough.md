# Звіт про виконану роботу: Виділення цін у гривні жирним шрифтом (v0.7.18)

## Огляд задачі
Користувач попросив виділити ціни у гривнях жирним шрифтом для максимальної зручності читання в повідомленнях.

---

## Виконані кроки

1. **Оновлено форматування цін**:
   - `services/eshop/formatters.py` та `eshop-prices/src/bot/formatters.py`:
     - Усі конвертовані ціни в гривнях тепер огортаються в тег `<b>~XXX грн</b>`:
       - У базовому рядку ціни: `(<b>~435 грн</b> / $9.72)`
       - У рядках регіональних цін топ-3 та закріплених країн: `(<b>~270 грн</b> / $6.04)`.

2. **Тестування**:
   - Усі 20 тестів пройшли успішно (`pytest -v -n auto`).
