# Звіт про виконану роботу: Суворе обмеження видалення виключно топіком 561344 (v0.7.07)

## Огляд задачі
Користувач зазначив, що видалення повідомлень має право відбуватися **виключно і тільки в топіку знижок https://t.me/kefir_ukr/561344** (`chat_id: -1001790782971`, `topic_id: 561344`). Жодне повідомлення з інших гілок форуму (наприклад, теми роздач `25501` чи Загального чату) ніколи не повинно видалятися.

---

## Виконані кроки

1. **Жорсткий захисний бар'єр (Security Guardrails)**:
   - `send_eshop_deals.py` та `services/eshop/bot_commands.py`:
     - Додано перевірку, яка блокує виклик `bot.delete_message`, якщо чат або топік не є `561344` у групі `-1001790782971`.
     - Повністю вимкнено будь-яке "сліпе" сканування числових діапазонів `message_id` супергрупи, щоб випадково не зачепити повідомлення з інших тем.
     - Видаляються виключно точні картки ігор вітрини, зафіксовані в базі для топіка `561344`.

2. **Тестування**:
   - Усі 17 тестів пройшли успішно (`pytest -v -n auto`).
