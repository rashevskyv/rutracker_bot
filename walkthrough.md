# Звіт про виконану роботу: Додавання відображення цін у гривнях (UAH) (v0.6.73)

## Огляд задачі
Користувач поставив задачу: окрім місцевих валют і цін у доларах ($), показувати еквівалент вартості в **українській гривні (грн / ₴)** за актуальним курсом.

---

## Виконані кроки

1. **Конвертація валют (`CurrencyService`)**:
   - Додано підтримку курсу `UAH` до `FALLBACK_USD_RATES` (41.50) та автоматичне завантаження live-курсу гривні з `open.er-api.com`.
   - Реалізовано метод `convert_to_uah(amount, from_currency) -> float`.

2. **Оновлення моделей (`RegionalPrice`)**:
   - Додано поле `converted_uah: float = 0.0`.
   - В `RegionPriceService` проводиться автоматичний розрахунок `converted_uah` при отриманні цін з усіх 12+ регіонів Nintendo eShop.

3. **Форматування повідомлень (`formatters.py`)**:
   - Картки товарів тепер містять одночасно еквівалент у гривнях та доларах:
     `🥇 🇳🇿 New Zealand: 3.29 NZD (-85%) (~80 грн / $1.94)`
     `🇵🇱 Poland: 7.99 PLN (-85%) (~89 грн / $2.15)`
     `🇺🇸 USA: 2.99 USD (-70%) (~124 грн / $2.99)`

4. **Тестування**:
   - Оновлено тести в `test_eshop_module.py`.
   - Усі 10 паралельних тестів пройшли успішно (`pytest -v -n auto`).
